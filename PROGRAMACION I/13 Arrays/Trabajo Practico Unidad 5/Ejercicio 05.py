# 5) Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza.

numeros = [8, 15, 3, 22, 7]
numeros.remove(max(numeros))
print(numeros)

# Este programa Encuentra el valor maximo de una lista, En este caso seria 22,
# Luego a traves de remove lo que hace es eliminar este valor y luego
# imprime la lista sin el 22, es decir: [8,15,3,7]
